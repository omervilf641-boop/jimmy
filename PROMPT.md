# 🎯 פרומט הבנייה של ג'ימי (Jimmy Build Brief)

> פרומט זה הוא המפרט הרשמי לבניית ג'ימי. הוא נכתב כדי שאפשר יהיה להריץ אותו מההתחלה ולקבל סוכן עובד, בלי עצירות באמצע.

---

## המשימה

בנה את **ג'ימי** — סוכן AI אישי שלומד מהמשתמש שלו ועוזר לו באמת.

ג'ימי הוא לא צ'אטבוט עם תשובות מוכנות מראש. הוא סוכן עם מוח אמיתי (Claude API), עם זיכרון מתמשך שנשמר בין הפעלות, והוא נעשה שימושי יותר ככל שמדברים איתו יותר — כי הוא זוכר מי אתה, מה לימדת אותו, ואיך אתה אוהב שיענו לך.

---

## מה ג'ימי חייב לדעת לעשות

### 1. מוח אמיתי
- מדבר דרך **Claude API** (`anthropic` SDK), לא לוגיקת `if/else`.
- יש לו אישיות: חברותי, ישיר, סקרן, עוזר בפועל ולא רק מדבר.
- **אם אין `ANTHROPIC_API_KEY` — הוא לא קורס.** הוא עובר למצב אופליין עם תשובות מבוססות כללים, אומר את זה למשתמש בבירור, וכל שאר המערכות (זיכרון, עובדות, כישורים) ממשיכות לעבוד רגיל.

### 2. זיכרון שבאמת משפיע
- כל שיחה, עובדה, כישור והעדפה נשמרים ל-`memory.json` ושורדים הפעלה מחדש — **כולל** מה שנטען מהדיסק, לא רק מה שנלמד בריצה הנוכחית.
- לפני כל תשובה ג'ימי **שולף מהזיכרון את מה שרלוונטי** ומזריק את זה להקשר של המודל. זיכרון שלא משפיע על התשובה הוא באג, לא פיצ'ר.
- שליפה חכמה: התאמה לפי מילות מפתח + טריות + חשיבות, לא רק חפיפת מילים גולמית.

### 3. למידה אמיתית
- `teach [עובדה]` — לומד עובדה מפורשת.
- `learn skill [כישור]` — רוכש כישור, והמיומנות בו **עולה בפועל** עם השימוש. אם יש פונקציה שמשפרת מיומנות — משהו חייב לקרוא לה.
- **למידה אוטומטית**: ג'ימי מזהה מידע על המשתמש מתוך שיחה רגילה ("קוראים לי...", "אני עובד ב...", "אני מעדיף...") ושומר אותו בלי שיבקשו ממנו.
- בלי כפילויות: ללמד את אותו דבר פעמיים מעדכן רשומה קיימת, לא יוצר חדשה.

### 4. עוזר בפועל
- זוכר את השם, ההעדפות וההקשר של המשתמש ומשתמש בהם בתשובות.
- `stats` / `progress` / `memory` — מראה מה הוא יודע ואיפה הוא עומד.
- `forget [משהו]` — המשתמש יכול למחוק מידע שהוא לא רוצה שיישמר. שליטה בזיכרון היא חובה.
- `export` — ייצוא הזיכרון לקובץ קריא.

---

## דרישות הנדסיות

- **Python 3.8+**, קוד נקי עם type hints ו-docstrings.
- `requirements.txt` מכיל **רק** מה שבאמת מיובא בקוד. אין תלויות פיקטיביות.
- אין `except:` חשוף — תמיד לתפוס חריגה ספציפית.
- `.gitignore` חוסם את `memory.json` ואת `.env` — זיכרון פרטי לא נכנס לגיט לעולם.
- **טסטים** שמכסים: שמירה וטעינה של זיכרון, שרידות אחרי restart, למידת עובדות וכישורים, עליית מיומנות, מניעת כפילויות, ומצב אופליין בלי מפתח API.
- `README.md` מעודכן שמתאר את מה שקיים בפועל — כל הבטחה בו חייבת להיות מגובה בקוד.

---

## קריטריוני סיום (Definition of Done)

המשימה נגמרת רק כששת אלה נכונים בו-זמנית:

1. ✅ `python jimmy.py` עולה ועובד — גם עם מפתח API וגם בלעדיו.
2. ✅ אחרי `exit` והפעלה מחדש, ג'ימי זוכר את השם, העובדות והכישורים שלמד.
3. ✅ כל הטסטים עוברים.
4. ✅ אפס באגים ידועים מתוך הרשימה במפרט הזה.
5. ✅ ה-README משקף את המציאות.
6. ✅ הכל מקומיט ונדחף לענף.

---

## 🔴 כלל העבודה — הכי חשוב

**אל תעצור באמצע.**

- נתקלת בבעיה? **תקן אותה ותמשיך.** אל תדווח עליה ותחכה.
- טסט נכשל? תקן, הרץ שוב, תמשיך.
- משהו לא עובד כמו שציפית? תבין למה, תשנה גישה, תמשיך.
- אל תשאל שאלות על דברים שאפשר להחליט עליהם לבד — תחליט, תתעד את ההחלטה, ותמשיך.
- אל תדווח "סיימתי" לפני שכל קריטריוני הסיום ירוקים בפועל, לא בתיאוריה.

עצור רק אם אתה חסום באמת ואין שום דרך להתקדם — ואז תגיד בדיוק מה חוסם ומה אתה צריך.

**תסיים את המשימה לגמרי.**
